# Spec Kitty Legacy Archive

Date: 2026-05-29

This archive keeps Spec Kitty material that should not be part of the active
project surface, but should remain available for audit or recovery.

- `my-spec-project/`: duplicate Spec Kitty initialization created inside the
  monorepo. It has no mission artifacts and duplicates generated `.kittify/`
  and `.agents/` files from the repository root.
- `product-detail-ux-01kspdtl-01KSTR1T/`: incomplete mission stub that only had
  metadata and an empty `spec.md`. It was moved out of `kitty-specs/` so future
  missions do not see it as an active mission.

The active Spec Kitty surface remains the repository root `.kittify/`,
`.agents/skills/`, and `kitty-specs/mvp-stabilization-01KSKEFD/`.
